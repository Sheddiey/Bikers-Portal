import { lusitana } from '@/app/ui/font';
 
export default async function Page () {
    return (
        <main>
            <h1 className={`${lusitana.className} mb-4 text-xl md:text-2xl`}>View Topic</h1>
            
        </main>
    );
}